spring:
  application:
    name: exampleRedisKa

  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:mem:rediska;CACHE_SIZE=0
    driver-class-name: org.h2.Driver
    username: admin
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

  data:
    redis:
      host: localhost
      port: 6379


  cloud:
    aws:
      region:
        static: ru-central1
      credentials:
        access-key: YCAJEdQAUZGFpUJG0XfWH0tyq
        secret-key: YCPG3ui0NGEcjiIovjHOeGaElh4XXyHruiUX6hiU